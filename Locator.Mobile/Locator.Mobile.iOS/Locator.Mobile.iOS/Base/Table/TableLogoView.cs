/*using MonoTouch.UIKit;
using XibFree;

namespace Locator.Mobile.iOS.Base.Table
{
    public class TableLogoView : FrameLayout
    {
        public TableLogoView()
        {
            LayoutParameters = new LayoutParameters
            {
                Width = AutoSize.FillParent,
                Height = AutoSize.FillParent,
                Gravity = Gravity.TopCenter
            };

            Padding = new UIEdgeInsets(10, 0, 20, 0);

            AddSubView(
                new UIImageView(Assets.Footer()) {ContentMode = UIViewContentMode.Center},
                new LayoutParameters(AutoSize.FillParent, AutoSize.WrapContent)
                );
        }
    }
}*/